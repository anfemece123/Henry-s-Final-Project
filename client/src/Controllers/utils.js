// filter els of arr2 and els of arr1
/* const filterProducts = (arr1, arr2) => {
  console.log(arr1);
  const arrOfFiltered = arr2.filter((prod) => arr1.includes(prod));
  console.log("filtrados", arrOfFiltered);
  return arrOfFiltered;
};

export default filterProducts;
 */
